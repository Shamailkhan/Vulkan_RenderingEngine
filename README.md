# Vulkan_RenderingEngine
A reusable Vulkan rendering engine  to be integrated into different projects and applications. The engine provides the core rendering infrastructure required to handle graphics rendering through Vulkan, including GPU resource management, command buffers, pipelines, shaders, textures, synchronization, cameras, lighting.
